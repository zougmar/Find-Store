{
  "name": "find-store",
  "version": "1.0.0",
  "description": "Find Store e-commerce application",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

